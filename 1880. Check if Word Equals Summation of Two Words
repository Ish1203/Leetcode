bool isSumEqual(char* firstWord, char* secondWord, char* targetWord) {
    int value(char* str){
        int v=0;
        for(int i=0;str[i];i++) v=v*10+(str[i]-'a');
        return v;
    }
    return value(firstWord)+value(secondWord)==value(targetWord);
}
